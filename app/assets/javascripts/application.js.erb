// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, vendor/assets/javascripts,
// or any plugin's vendor/assets/javascripts directory can be referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file.
//
// Read Sprockets README (https://github.com/rails/sprockets#sprockets-directives) for details
// about supported directives.
//
//= require jquery
//= require jquery_ujs
//= require jquery-ui
//= require turbolinks
//= require twitter/bootstrap/modal
//= require mustache
//= require retina
//= require waypoints
//= require parallax
//= require jquery.mixitup
//= require jquery.easing
//= require jquery.fittext
//= require jquery.appear
//= require jquery.waitforimages
//= require jquery.bxslider
//= require jquery.fitvids
//= require jquery.easings.min
//= require scrolloverflow.min.js
//= require jquery.fullPage
//= require main
//= require shortcodes
//= require particles
//= require jquery.slicknav
//= require_tree ../../templates

jQuery(document).ready(function() {
	
	
  	particlesJS.load('particles-js', '/assets/particlesjs-config.json', function() {
  		console.log('callback - particles.js config loaded');
  	});
	
	jQuery(function(){
		$('#menu').slicknav();
	});
	
	//// Preloader, hide all page content until window.load
	jQuery('.loadingGif').show();
	
	jQuery('#fullpage').fullpage({
		anchors:['section1', 'section2', 'section3', 'section4', 'section5'],
		
		afterLoad: function(anchorLink, index){
			var loadedSection = $(this);
			//using index
		    	if(index == 1 || index == 3 || index == 4) {
					$('#naviMenu a').animate({ color: "#fff" }, 500);
					$(".logo").toggleClass("blackLogo whiteLogo");
		        } else if (index == 2 || index == 5) {
					$('#naviMenu a').animate({ color: "#000" }, 500);
					$(".logo").toggleClass("whiteLogo blackLogo ");
				}
				if (index == 1) {
				    $('.menuWrapper li.logoWrapper').css({"top":"60px"});
					$('.menuWrapper').css({"margin-top":"72px"});
					$('.navContainer').css({"height":"192px"});
				}
				else {
			        $('.menuWrapper li.logoWrapper').css({"top":"24px"});
					$('.menuWrapper').css({"margin-top":"36px"});
					$('.navContainer').css({"height":"114px"});
				}
		}
	});
	
});


//// Load page content once it has been preloaded
jQuery(window).load(function() {
	jQuery('.loadingGif').hide();
  	jQuery('.body').fadeIn(300);
});
jQuery(window).on('scroll', function() {
    var scrollTop = $(this).scrollTop();
    $('.page').each(function() {
        var topDistance = $(this).offset().top - 100;
		var colour = $(this).attr('data-color');
        if ( (topDistance) < scrollTop ) {
			$('#naviMenu a').css('color',colour);
			if (colour == "#000") {
				$('.logo').addClass('blackLogo');
			}
			else {
				$('.logo').removeClass('blackLogo');
				$('.logo').addClass('whiteLogo');
			}
        }
    });
	// var targetOffset = $("#section2").offset().top -100;
	// 	var $w = $(window).scroll(function(){
	// 	    if ( $w.scrollTop() > targetOffset ) {
	// 	    }
	// 	});
});